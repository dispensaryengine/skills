# Modular Skill Package Authoring

Use this package to create or refactor skills into the same modular system.

Start with `SKILL.md`.

The validator can be run with:

```bash
python tools/validate_skill_package.py /path/to/skill-package
```
