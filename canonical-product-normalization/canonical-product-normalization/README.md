# Canonical Product Normalization

Start with `SKILL.md`.

The uploaded `normalization_foundation.md`, `structure_contract.md`, and `style_contract.md` are preserved as authoritative references.

The corrected controller no longer defaults implementation requests to PDF output. Formal document styling is loaded only when a formal artifact is requested.

Detailed execution order is in `references/workflow_contract.md`.
