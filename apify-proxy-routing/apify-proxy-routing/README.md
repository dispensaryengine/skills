# Apify Proxy Routing

Start with `SKILL.md`.

This package treats Apify as a proxy provider and does not require Apify Actors.

Current platform behavior should be reverified against official Apify documentation when implementation depends on it.

Detailed execution order is in `references/workflow_contract.md`.
